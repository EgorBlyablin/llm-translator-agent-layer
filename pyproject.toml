[project]
name = "networking-llm-layer"
requires-python = ">=3.14"
dynamic = ["version"]
dependencies = [
    "fastapi[standard]>=0.135.3",
    "httpx>=0.28.1",
    "openai>=2.33.0",
    "pydantic>=2.12.5",
    "pyyaml>=6.0.3",
    "uvicorn>=0.43.0",
]

[tool.ty.environment]
python = ".venv"
